spring:
  application:
    name: user-service

  datasource:
    username: admin
    password: password
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb

  h2:
    console:
      enabled: true
      path: /h2-console

  security:
    user:
      name: admin
      password: test-password