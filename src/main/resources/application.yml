server:
  port: 8080

spring:
  application:
    name: User-Service

  datasource:
    username: admin
    password: password
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb

  h2:
    console:
      enabled: true
      path: /h2-console

  security:
    user:
      name: admin
      password: ${ADMIN_PASSWORD}