apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  type: LoadBalancer
  selector:
    app: user-service
  ports:
    - name: http
      protocol: TCP
      port: 80         # external port
      targetPort: 8080 # matches containerPort
